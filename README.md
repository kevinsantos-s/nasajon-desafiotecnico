# Desafio Técnico — Nasajon

## Como rodar

npm install

Copie o `.env.example` para `.env` e preencha o `ACCESS_TOKEN`:

Execute:

npx tsx src/index.ts

O `resultado.csv` será gerado na raiz do projeto.
